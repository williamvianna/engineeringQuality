import { Inter,  Saira} from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
});

export const saira = Saira({
    subsets: ['latin'],
    display: 'swap',
});
