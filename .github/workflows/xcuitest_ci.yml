name: 📱 CI - Testes XCUITest/iOS

on:
  push:
    branches: [ main, develop ] # Roda o CI ao commitar na main ou na develop
  pull_request:
    branches: [ main ] # Roda o CI também quando você abrir um PR para juntar a develop na main
    paths:
      - 'xcuitest/**'
      - '.github/workflows/xcuitest_ci.yml'

jobs:
  run_xcuitest:
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
      - name: ⬇️ Checkout do Código
        uses: actions/checkout@v5

      # - name: 🏃 Rodar Testes XCUITest com xcodebuild
      #   run: |
      #     xcodebuild clean test \
      #     -project xcuitestProject.xcodeproj \
      #     -scheme MyApplicationsUITests \
      #     -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest'
      #   working-directory: xcuitest